package enums

enum class Enums(val value: String) {
    APP_DIRECTORY("/KVault"),
    HOME_DIR("user.home"),
    KEY_DIR("/Keys"),
    VAULTS_DIR("/Vaults"),
    NO_VAULTS_MSG("Open Or Create A Vault To Get Started")
}